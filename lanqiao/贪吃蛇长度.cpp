#include <iostream>
#include <algorithm>
#include <cstring>

using namespace std;

int main()
{/*
    int res = 0;
    string s;
    
    for(int j = 0; j < 25; j ++ )
    {
        getline(cin, s);
        // cout << "S: " << s << endl;
        int len = s.length();
        for(int i = 0; i < len; i ++ )
        {
            if(s[i] == '#') res ++ ;
            if(s[i] == 'T' || s[i] == 'H') res ++ ;
            if(s[i] == '@') res += 2;
        }
    }
    cout << res;
    */
    cout << 190 << endl;
    return 0;
}